# Cerberus Defense Suite (MVP)

AI-powered cybersecurity framework with:
- Phishing Decoy Engine
- Behavioral Biometrics
- BEC Detection

Backend: FastAPI  
Frontend: React  
Deployment: Docker / Kubernetes  

## 🚀 Setup
1. `cd backend`
2. `python -m venv venv && source venv/bin/activate`
3. `pip install -r requirements.txt`
4. `uvicorn app.main:app --reload`

### ✅ Accessibility (Best PWD Eligible)
- Screen-reader friendly alerts (`aria-live`)
- Semantic HTML in decoy pages
- High-contrast mode support (CSS `@media (prefers-contrast: high)`)