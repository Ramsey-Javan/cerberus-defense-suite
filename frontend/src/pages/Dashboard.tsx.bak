// src/pages/Dashboard.tsx
import { useState, useEffect } from 'react';
import { Link, Outlet, useLocation } from 'react-router-dom';
import GlassCard from '../components/ui/GlassCard';

const navItems = [
  { name: 'Dashboard', path: '/dashboard' },
  { name: 'Alerts', path: '/dashboard/alerts' },
  { name: 'Attack View', path: '/dashboard/attack' },
];

export default function Dashboard() {
  const [sidebarOpen, setSidebarOpen] = useState(false);
  const location = useLocation();

  // Close mobile sidebar on route change
  useEffect(() => {
    setSidebarOpen(false);
  }, [location]);

  return (
    <div className="min-h-screen bg-gray-950 text-gray-100 flex">
      {/* Desktop Sidebar */}
      <aside className="hidden md:flex md:flex-col w-64 bg-gray-900 border-r border-gray-800 p-4">
        <div className="mb-8">
          <h1 className="text-2xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-indigo-400 to-cyan-400">
            Cerberus
          </h1>
          <p className="text-gray-500 text-sm mt-1">Defense Suite</p>
        </div>

        <nav className="space-y-1 flex-1">
          {navItems.map((item) => (
            <Link
              key={item.path}
              to={item.path}
              className={`flex items-center px-4 py-3 rounded-lg transition-colors ${
                location.pathname === item.path
                  ? 'bg-indigo-900/30 text-white border-l-2 border-indigo-500'
                  : 'text-gray-300 hover:bg-white/5 hover:text-white'
              }`}
            >
              <span className="ml-3 font-medium">{item.name}</span>
            </Link>
          ))}
        </nav>

        <div className="mt-auto pt-4 border-t border-gray-800">
          <div className="text-xs text-gray-500">v1.0.0 • MVP</div>
        </div>
      </aside>

      {/* Mobile Sidebar Toggle */}
      <button
        onClick={() => setSidebarOpen(!sidebarOpen)}
        className="md:hidden fixed top-4 left-4 z-20 p-2.5 rounded-lg bg-gray-800/80 backdrop-blur-sm text-white"
        aria-label={sidebarOpen ? 'Close menu' : 'Open menu'}
      >
        <svg className="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          {sidebarOpen ? (
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M6 18L18 6M6 6l12 12" />
          ) : (
            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M4 6h16M4 12h16M4 18h16" />
          )}
        </svg>
      </button>

      {/* Mobile Sidebar (Overlay) */}
      {sidebarOpen && (
        <div
          className="md:hidden fixed inset-0 z-10 bg-black/60 backdrop-blur-sm"
          onClick={() => setSidebarOpen(false)}
          role="dialog"
          aria-modal="true"
        >
          <div
            className="w-64 h-full bg-gray-900 border-r border-gray-800 p-4"
            onClick={(e) => e.stopPropagation()}
          >
            <nav className="mt-8 space-y-1">
              {navItems.map((item) => (
                <Link
                  key={item.path}
                  to={item.path}
                  className={`block px-4 py-3 rounded-lg transition-colors ${
                    location.pathname === item.path
                      ? 'bg-indigo-900/30 text-white'
                      : 'text-gray-300 hover:bg-white/5 hover:text-white'
                  }`}
                >
                  {item.name}
                </Link>
              ))}
            </nav>
          </div>
        </div>
      )}

      {/* Main Content */}
      <main className="flex-1 p-4 md:p-6 overflow-auto bg-gradient-to-br from-gray-900 to-gray-950">
        <div className="max-w-7xl mx-auto">
          {/* Dynamic Page Header */}
          <header className="mb-6">
            <h1 className="text-3xl font-bold text-white">
              {navItems.find(item => item.path.includes(location.pathname))?.name || 'Dashboard'}
            </h1>
            <p className="text-gray-400 mt-1">
              Real-time threat intelligence and decoy engagement analytics
            </p>
          </header>

          {/* Stats Grid */}
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <GlassCard title="Live Alerts">
              <div className="text-3xl font-bold text-red-400">0</div>
              <div className="text-gray-400 text-sm mt-2">Last 5 min</div>
            </GlassCard>

            <GlassCard title="Active Decoys">
              <div className="text-3xl font-bold text-amber-400">0</div>
              <div className="text-gray-400 text-sm mt-2">In progress</div>
            </GlassCard>

            <GlassCard title="BEC Detections">
              <div className="text-3xl font-bold text-purple-400">0</div>
              <div className="text-gray-400 text-sm mt-2">Pending review</div>
            </GlassCard>

            <GlassCard title="Risk Score">
              <div className="flex items-baseline">
                <span className="text-3xl font-bold text-cyan-400">0</span>
                <span className="text-gray-500 ml-2">/100</span>
              </div>
              <div className="text-gray-400 text-sm mt-2">System-wide</div>
            </GlassCard>
          </div>

          {/* Content Grid */}
          <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
            <div className="lg:col-span-2 space-y-6">
              <GlassCard title="Latest Alerts">
                <div className="space-y-4">
                  {[1, 2, 3].map((i) => (
                    <div key={i} className="flex items-start pb-4 border-b border-gray-800 last:border-0 last:pb-0">
                      <div className="w-2 h-2 rounded-full bg-red-500 mt-2 mr-3 flex-shrink-0"></div>
                      <div>
                        <div className="font-medium text-gray-200">Phishing decoy triggered</div>
                        <div className="text-sm text-gray-500">2 min ago • 192.168.1.105</div>
                      </div>
                    </div>
                  ))}
                </div>
              </GlassCard>

              <GlassCard title="BEC Detections">
                <div className="text-center py-8 text-gray-500">
                  No suspicious transfers detected
                </div>
              </GlassCard>
            </div>

            <div className="space-y-6">
              <GlassCard title="Top Targeted Users">
                <ul className="space-y-3">
                  {[{ user: 'jdoe@company.com', count: 3 }, { user: 'admin@company.com', count: 2 }].map((item, i) => (
                    <li key={i} className="flex justify-between items-center">
                      <span className="text-gray-300">{item.user}</span>
                      <span className="bg-gray-800 px-2 py-1 rounded text-sm">{item.count} attempts</span>
                    </li>
                  ))}
                </ul>
              </GlassCard>

              <GlassCard title="Attacker Fingerprint">
                <div className="space-y-3 text-sm">
                  <div className="flex justify-between">
                    <span className="text-gray-400">Browser</span>
                    <span className="text-gray-200">Chrome 120</span>
                  </div>
                  <div className="flex justify-between">
                    <span className="text-gray-400">OS</span>
                    <span className="text-gray-200">Windows</span>
                  </div>
                  <div className="flex justify-between">
                    <span className="text-gray-400">IP</span>
                    <span className="text-gray-200">203.***.***.142</span>
                  </div>
                </div>
              </GlassCard>
            </div>
          </div>

          {/* 👇 Renders child routes: /dashboard/alerts, /dashboard/attack, etc. */}
          <Outlet />
        </div>
      </main>
    </div>
  );
}